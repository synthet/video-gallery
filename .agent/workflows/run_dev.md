---
description: Start the Electron app in development mode (Vite HMR + Electron)
---

> **Canonical workflow:** prefer [run_video_dev.md](run_video_dev.md) for up-to-date, path-agnostic steps.

## Quick start (repo root)

```bash
npm install
npm run dev
```

## Prerequisites

- Node and npm installed.
- PostgreSQL / backend reachable per `config.json` when testing real data.

See [run_video_dev.md](run_video_dev.md) for `dev:web`, Linux, and troubleshooting.
